<table>
    <h1>All Songs:</h1>
    <tbody>
        <th>Title</th><br>
        <% @songs.each do |song| %>   
        <tr>
            <td><%= link_to song.title, song_path(song) %></td>
            <% end %>
        </tr>
    </tbody>
</table>